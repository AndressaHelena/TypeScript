for(let tabuada:number = 1; tabuada <= 10; tabuada++){ 
    console.log(`\nTabuada do ${tabuada}`);
    for(let numero = 1; numero <= 10; numero++){
        console.log(`${numero} x ${tabuada} = ${numero * tabuada}`);
      continue;
    
    }
}